<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/iv_movie_poster"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:contentDescription="@string/default_image_desc"
        android:src="@mipmap/no_poster_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_movie_poster"
        style="@style/TextContent"
        android:layout_margin="@dimen/indent_none"
        android:background="@color/colorPosterTitleBackground"
        android:padding="@dimen/indent_one_step"
        android:text="@string/default_image_desc"
        app:layout_constraintBottom_toBottomOf="@id/iv_movie_poster" />
</android.support.constraint.ConstraintLayout>